# CareerConnect

This project is now organized into two top-level folders:

- `frontend/` for the React + Vite client
- `backend/` for the Node.js + Express API

## Run the frontend

```bash
cd frontend
npm install
npm run dev
```

## Run the backend

```bash
cd backend
npm install
npm run dev
```
